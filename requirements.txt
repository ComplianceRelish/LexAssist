# === LexAssist Backend Dependencies ===

# Web framework
flask>=2.3,<4.0
flask-cors>=4.0,<5.0
gunicorn>=21.2,<23.0

# Supabase
supabase>=2.0,<3.0

# Auth / JWT
PyJWT>=2.8,<3.0

# HTTP
requests>=2.31,<3.0

# AI — Claude (Anthropic) — >=0.45 needed for Sonnet 4.5
anthropic>=0.45,<1.0

# AI — OpenAI (Whisper STT + Vision OCR)
openai>=1.12,<2.0

# Document Processing (Scanner / OCR pipeline)
Pillow>=10.0,<12.0
PyMuPDF>=1.23,<2.0
python-docx>=1.0,<2.0

# Environment
python-dotenv>=1.0,<2.0

# NLP (optional — set ENABLE_INLEGALBERT=true to use)
# transformers>=4.35
# torch>=2.1

# Build tools (ensures pkg_resources is available)
setuptools>=69.0

# Utilities
markupsafe>=2.1
